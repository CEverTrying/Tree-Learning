# TreeLearning 树学 · Windows

适用于 Windows 10 / 11 x64，无需安装 Node.js 或单独启动服务。

## 安装和启动

最新版本为 `release/windows/TreeLearning-0.1.8-Windows-x64.zip`，本次仅生成免安装压缩包。完整解压后运行其中的 `TreeLearning.exe`。以下安装向导步骤仅适用于此前的安装版。

1. 打开安装包，按中文向导选择安装目录。
2. 安装到当前用户目录，无需管理员权限。
3. 完成后打开 TreeLearning；也可使用桌面或开始菜单中的“TreeLearning 树学”。

免安装版请保留解压后的完整目录，不要只移动 EXE 文件.

当前版本未签名，Windows 可能将发布者显示为未知。正式发布所需的代码签名证书尚未配置。

## 已验证范围

0.1.9 已通过源码构建，包内版本及构建文件一致性检查；相关界面修改已通过浏览器流程验证。本次打包环境的 WSL → Windows 互操作返回 `UtilBindVsockAnyPort: socket failed`，未能启动 0.1.9 的 Windows 实机验证。以下桌面验证记录来自此前版本。

已在 Windows 11（系统版本 10.0.26200）运行打包后的 `TreeLearning.exe`，通过中文菜单、窗口沙箱、中文文件导入、问答、完整祖先上下文、公式显示、叶节点编辑、单实例、退出重开恢复数据和历史节点锁定验证。界面截图见 [windows-app.png](screenshots/windows-app.png)。

安装向导的交互安装与卸载尚未实测，Windows 10 尚未单独实测。模型接口使用本地模拟服务验证，未使用真实付费模型。

## 开始学习

先在根节点设置全局基础知识，再创建项目。项目学习内容可留空。AI 的访问范围严格限定在当前节点及全部祖先。文件只提供名称、标识和长度，正文按需读取；其他分支的文件不可访问，即使属于同一项目。

有子节点时锁定编辑；叶节点支持修改问题和回复，问题旁可点击“修改发送内容”，保存后可重新生成回复。非根节点可以删除，确认后全部子节点一并删除；删除全部子节点后父节点恢复编辑。从历史节点继续提问会创建新的子分支。

左侧列表仅显示当前节点、直接父节点和直接子节点，点击后随当前节点切换；搜索仅匹配直接子节点。学习树图从左向右横向生长，只显示当前所属项目及其后代，根节点页面显示项目列表。

默认 **AI 模式**。在左下角“模型设置”填写 API 地址、模型名称和密钥后即可使用真实 AI；未配置时会提示先配置。

“模型设置”可切换 **AI 模式 / 演示模式 / 调试模式**。演示模式不调用 AI；调试模式正常调用 AI，同时在顶部提供“查看 API 请求”入口，可查看和复制完整请求正文、导出请求，以及查看请求地址、请求头和成功、失败、取消状态。请求头中的密钥会遮蔽。

调试记录只在本次运行保留最近 20 次，退出程序或离开调试模式后清除。升级会保留旧版已保存的模式选择，可在设置中切换。

## AI 阅读分支文件


通过“添加文件”创建教材节点，然后在该文件下提问或从它的后代对话继续，例如“查阅这份教材，解释线性组合”。AI 会按需列出路径内文件、查找关键词并读取相关正文。模型需要支持 Function Calling；演示模式不执行文件工具。

工具只能访问当前节点到根节点这条路径中的文件，不能读取兄弟分支、当前节点的后代、其他项目或任意本机路径。支持文本型 PDF、DOCX、Markdown、TXT、CSV、JSON 和代码；扫描件需先 OCR。

回答下方“本次阅读”可展开实际读取的片段。片段随回答保存供查看和备份，不自动加入后续上下文；需要时由 AI 再次读取。旧版已有记录同样不自动发送，历史问题和回复保持不变。调试模式能查看每轮 API 请求及工具参数和返回内容。

## 本机数据

- 菜单“文件 → 打开数据文件夹”可以直接找到数据，默认目录是 `%APPDATA%/treelearning/`。
- `tree.json` 保存学习树；`documents/` 保存文件原件和正文；`settings.json` 保存模型配置。迁移数据时需要一起保留。
- 密钥默认仅保存在本次运行的内存中。勾选保存密钥后会明文写入设置文件。
- 0.1.6 左下角可导出包含文件的完整 ZIP 备份，也兼容恢复旧 JSON。备份不包含 API 密钥。
- 正常退出会等待已经提交的本机写入完成。再次运行会载入原来的学习树。
- 卸载程序默认保留学习数据。移动免安装目录也不会移动或删除学习数据。
- 启动或窗口异常会记录到数据目录的 `desktop.log`。

## 从源码构建

在 Windows 安装 Node.js 22.19 或更新版本，然后在源码目录执行：

```powershell
npm ci
npm run dist:win
```

输出在 `release/`，包含安装器、免安装压缩包和 `win-unpacked/` 目录。

运行桌面验证：

```powershell
$env:TREELEARNING_EXECUTABLE = "$PWD\release\win-unpacked\TreeLearning.exe"
npm run test:desktop
```

测试使用独立数据目录，覆盖中文菜单、中文文件导入、问答、祖先上下文、公式渲染、叶节点编辑、单实例启动、退出重开和历史锁定。
